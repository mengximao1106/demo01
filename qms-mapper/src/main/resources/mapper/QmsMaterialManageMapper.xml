<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.evergrande.qms.mapper.QmsMaterialManageMapper">
    <resultMap id="baseResultMap" type="com.evergrande.qms.model.QmsMaterialManage" >
        <id column="id" property="id" jdbcType="VARCHAR" javaType="java.lang.String" />
        <result column="material_code" property="materialCode" jdbcType="VARCHAR" javaType="java.lang.String" />
        <result column="material_name" property="materialName" jdbcType="VARCHAR" javaType="java.lang.String" />
    </resultMap>

    <sql id="Base_Column_List">
        id, material_code,create_user,create_date,update_user,update_date
    </sql>

    <select id="findList" resultMap="baseResultMap" parameterType="java.lang.String">
        select
        t.material_code,
        t1.material_name
        from qms_material_manage t LEFT JOIN qms_material t1 ON t.material_code=t1.material_code
        <where>
            <if test="materialCode!=null and materialCode!=''">
                and t1.material_code like concat('%', #{materialCode,jdbcType=VARCHAR},'%')
            </if>
            <if test="materialName!=null and materialName!=''">
                and material_name like concat('%', #{materialName,jdbcType=VARCHAR},'%')
            </if>
        </where>
        limit 200
    </select>
</mapper>